# Agentic service ownership boundary

This directory is reserved for the separately developed agentic service. The
LLM implementing that layer may modify only `services/agentic/**` and must follow
`docs/AGENTIC-SYSTEM-HANDOFF.md`.

The dashboard implementation does not live here and must not be copied into this
directory.
